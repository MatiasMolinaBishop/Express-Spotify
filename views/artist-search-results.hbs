<div class="artists-grid">
    {{#each artistInfo}}
    <div>
        <h1 class="artist-name">{{{this.name}}}</h1>
        <img class='artist-img' src="{{this.images.0.url}}">
        {{!-- .0. is the same as [0] to access the array. .0. is used on handle bars --}}
        <a class='view-albums' href="/albums/{{this.id}}">View Albums</a>
        {{!-- Here we are passing the id as a router param. As it is specified on the route --}}
    </div>
    {{/each}}
</div>